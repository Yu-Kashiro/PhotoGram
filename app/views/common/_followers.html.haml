.header
  .header-left
    = link_to 'javascript:history.back()', class: "back-button" do
      = image_tag 'back.png'
  .header-title Followers

- @followers.each do |follower|
  .follow-container
    .follow-box
      = image_tag follower.profile.display_avatar, class: 'profile-image'
      .follow-content
        .follow-name
          = follower.account_id
        .follow-login-time
          %span Last seen 3 hours ago
