.header{data: { account_id: @user.id}}
  .header-left
    = link_to :back, class: "back-button" do
      = image_tag 'back.png'
  .header-title
    = @user.account_id
  .header-right
    .unfollow-button.hidden Unfollow
    .follow-button.hidden Follow

.container
  .profile-header
    - if @profile&.avatar&.attached?
      = image_tag @profile.avatar, class: "profile-pic", id: "profile-pic"
    - else
      = image_tag 'dummy_profile.png', class: "profile-pic", id: "profile-pic"

    %input{ type: "file", id: "profile-pic-input", style: "display: none;" }

  .profile-stats
    .profile-stats-posts
      %h4 Posts
      = @post_count
    .profile-stats-followers
      %h4 Followers
      %span{class: "followers-count"}= @followers_count
    .profile-stats-followings
      %h4 Followings
      = @following_count

= javascript_pack_tag 'account'
